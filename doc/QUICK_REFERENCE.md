# Quick Reference: User Service URLs

## 🔗 Correct URLs to Use

### Swagger UI & Documentation
```
✅ Swagger UI:     http://localhost:8086/api/users/swagger-ui/index.html
✅ OpenAPI Docs:   http://localhost:8086/api/users/v3/api-docs
✅ Health Check:   http://localhost:8086/api/users/actuator/health
```

### API Endpoints
```
✅ Create User:    POST http://localhost:8086/api/users/users
✅ List All Users: GET  http://localhost:8086/api/users/users/all
```

## ❌ Common Mistakes

```
❌ WRONG: http://localhost:8086/swagger-ui/index.html
✅ RIGHT: http://localhost:8086/api/users/swagger-ui/index.html

❌ WRONG: http://localhost:8086/users
✅ RIGHT: http://localhost:8086/api/users/users

❌ WRONG: POST without userId in request
✅ RIGHT: userId is auto-generated by the service (UUID)
```

## 📝 Request Example

```bash
curl -X POST http://localhost:8086/api/users/users \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "firstName": "John",
    "lastName": "Doe",
    "phoneNumber": "+1234567890",
    "profilePictureUrl": null,
    "isActive": true
  }'
```

## 🔑 Key Points

1. **Context Path**: `/api/users` is configured in application.yml
2. **Controller Base**: `/users` is in @RequestMapping
3. **Combined Path**: `/api/users/users` for user operations
4. **UUID**: Auto-generated by service, don't include in request
5. **Security**: User creation endpoint is public (no JWT needed)

## ✅ All Fixed Issues

- ✅ Swagger UI 404 → Context path corrected
- ✅ Auth service 404 → URL fixed to include full path  
- ✅ JPA persist error → UUID auto-generation added
- ✅ Repository error → Fixed return type
- ✅ Security config → Public access for Swagger & user creation
